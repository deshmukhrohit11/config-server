server:
  port: 0

synerzip:
  user:
    bu: BU-4
---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory-mgmt
    username: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: inventory-service
